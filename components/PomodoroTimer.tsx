import React from 'react';
import { Text, View } from 'react-native';

const PomodoroTimer: React.FC<{ navigation: any }> = ({ navigation }) => {
  return (
    <View>
      <Text>Timer</Text>
    </View>
  );
};

export default PomodoroTimer;
